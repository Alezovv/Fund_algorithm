#ifndef FLAGS_H
#define FLAGS_H

#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

void flag_d(FILE *infile, FILE *file);

void flag_i(FILE *infile, FILE *file);

void flag_s(FILE *infile, FILE *file);

void flag_a(FILE *infile, FILE *file);

#endif // FLAGS_H
